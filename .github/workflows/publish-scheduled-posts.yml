# Builds and deploys the Jekyll site directly so future-dated posts
# publish automatically on their scheduled date. Also runnable manually.
name: Publish scheduled posts

on:
  schedule:
    # GitHub Actions cron uses UTC. Run daily at both possible 8:05 AM Eastern
    # equivalents so daylight saving time is covered.
    - cron: '5 12 * * *'
    - cron: '5 13 * * *'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Check whether it is the 8 AM Eastern hour
        id: eastern_time
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          HOUR=$(TZ=America/New_York date +'%H')
          if [ "$EVENT_NAME" = "workflow_dispatch" ] || [ "$HOUR" = "08" ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Check out repository
        if: steps.eastern_time.outputs.run == 'true'
        uses: actions/checkout@v4

      - name: Configure GitHub Pages
        if: steps.eastern_time.outputs.run == 'true'
        uses: actions/configure-pages@v5

      - name: Build Jekyll site
        if: steps.eastern_time.outputs.run == 'true'
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site

      - name: Upload Pages artifact
        if: steps.eastern_time.outputs.run == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

      - name: Deploy to GitHub Pages
        if: steps.eastern_time.outputs.run == 'true'
        id: deployment
        uses: actions/deploy-pages@v4
